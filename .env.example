# Copy to .env and fill in your own values. Never commit the real .env.

# LLM calls (synthesis, review, chat)
OPENAI_API_KEY=

# External market-data adapters
FMP_API_KEY=
ALPHAVANTAGE_API_KEY=
FRED_API_KEY=

# Supabase (internal banking data: companies, credit exposures, CRM pipeline, risk flags)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_DB_URL=

# Only needed if a client reads Supabase directly with public env-prefixed vars
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
